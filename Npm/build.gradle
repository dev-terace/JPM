plugins {
    id 'java-library'
    id 'maven-publish'      // 배포를 위해 필수
    id 'java-gradle-plugin'
}

// 🔥 중요: group은 반드시 'com.github.본인아이디' 형식을 추천합니다.
group = 'com.github.dev_sj'
version = '1.0.0'

gradlePlugin {
    plugins {
        jpmPlugin {
            // 사용자가 plugins { id '...' } 에 적을 값
            id = 'com.github.dev_sj.jpm'
            implementationClass = 'config.plugin.JpmPlugin'
        }
    }
}

// JitPack이 빌드할 때 필요한 배포 설정
publishing {
    publications {
        mavenJava(MavenPublication) {
            from components.java
        }
    }
}