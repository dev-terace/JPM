plugins {
    id 'java'
    id 'java-library'
    id 'java-gradle-plugin'
    id 'maven-publish'
}

group = 'com.github.dev_sj'
version = '1.0.4'

repositories {
    mavenCentral()
}

gradlePlugin {
    plugins {
        jpmPlugin {
            id = 'com.github.dev_sj.jpm'
            // ⚠️ 여기가 핵심입니다!
            // 실제 패키지 명을 포함한 전체 경로여야 합니다.
            // 만약 config 패키지 안의 AppConfig라면 아래와 같이 수정:
            implementationClass = 'config.AppConfig'
        }
    }
}

// JAR 파일에 컴파일된 모든 클래스를 강제로 때려 넣는 설정 추가
jar {
    from sourceSets.main.output
    include "**/*.class"
}


dependencies {
    // 🔹 MyBatis Core (Mapper 생성용)
    implementation 'org.mybatis:mybatis:3.5.16'

    // 🔹 (선택) MyBatis-Spring — 필요 없으면 빼도 됨
    // implementation 'org.mybatis.spring:mybatis-spring:3.0.3'
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            from components.java
        }
    }
}
